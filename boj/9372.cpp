#include <iostream>
#include <vector>
#include <utility>
using namespace std;
#define x first
#define y second

using pint = pair<int, int>;

int main(int argc, char *argv[])
{
	cin.tie(0);
	cin.sync_with_stdio(false);

	int t;
	cin >> t;

	int n,m;
	for(int i = 0; i < t; i++) {
		cin >> n >> m;
	}
	return 0;
}
